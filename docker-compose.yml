version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nerdy-print-shop
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      VITE_APP_ID: nerdy-print-shop
      VITE_OAUTH_PORTAL_URL: https://oauth.manus.im
      DATABASE_URL: file:/app/data/nerdy_print_shop.db
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      OAUTH_SERVER_URL: https://oauth.manus.im
      OWNER_OPEN_ID: mateinorolamento89@gmail.com
      BUILT_IN_FORGE_API_URL: https://forge.manus.im
      BUILT_IN_FORGE_API_KEY: ${BUILT_IN_FORGE_API_KEY}
      WHATSAPP_PHONE_NUMBER: +5511953739362
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => {if (r.statusCode !== 200) throw new Error(r.statusCode)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # Opcional: MySQL para produção
  # mysql:
  #   image: mysql:8.0
  #   container_name: nerdy-print-shop-mysql
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
  #     MYSQL_DATABASE: nerdy_print_shop
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  #   restart: unless-stopped

volumes:
  # mysql_data:
  #   driver: local
